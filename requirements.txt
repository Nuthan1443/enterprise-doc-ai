# ── API Framework ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── LangChain ecosystem ────────────────────────────────────────
langchain>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-groq>=1.1.2
langchain-chroma>=0.2.0
langsmith>=0.2.0

# ── Vector store ───────────────────────────────────────────────
chromadb>=1.5.9

# ── Embeddings ─────────────────────────────────────────────────
sentence-transformers>=3.3.0


# ── Document loaders (replacing unstructured — too unstable) ───
pypdf>=5.1.0
python-docx>=1.1.2
pandas>=2.2.0
openpyxl>=3.1.0

# ── PII scrubbing ──────────────────────────────────────────────
presidio-analyzer>=2.2.356
presidio-anonymizer>=2.2.356
spacy>=3.8.0

# ── Evaluation ─────────────────────────────────────────────────
ragas>=0.4.3
datasets>=3.0.0

# ── Config management ──────────────────────────────────────────
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# ── Utilities ──────────────────────────────────────────────────
python-multipart>=0.0.12
aiofiles>=24.1.0

# requirements-dev.txt
# Install with: pip install -r requirements-dev.txt
pytest
